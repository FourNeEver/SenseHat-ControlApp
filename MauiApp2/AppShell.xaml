<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiApp2.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiApp2"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
            Title="HUB"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />

        <ShellContent
            Title="Connectivity"
            ContentTemplate="{DataTemplate local:NewPage1}"
            Route="NewPage" />

        <ShellContent
            Title="Data Table"
            ContentTemplate="{DataTemplate local:RawData}"
            Route="RawData" />
        <ShellContent
            Title="Data Graphs"
            ContentTemplate="{DataTemplate local:DataGraphs}"
            Route="DataGraphs" />
        <ShellContent
            Title="Led Control"
            ContentTemplate="{DataTemplate local:LedControl}"
            Route="LedControl" />
    </TabBar>
</Shell>
